# Render Deployment Configuration
NODE_ENV=production
PORT=10000
DATA_DIR=/opt/render/project/src/backend

# GitHub Auto-Backup (if you have a token)
GITHUB_TOKEN=${GITHUB_TOKEN}

# Database (for future use)
# DATABASE_URL=postgresql://...
